# 구현 계획 (implementation_plan.md)

본 문서는 **네이버 플레이스 영업 DB 수집기 V1**의 MVP 구현 방향을 정리합니다.

이번 V1은 PC 네이버 지도 iframe 구조가 아니라 **모바일 웹 `m.map.naver.com` 리스트 화면**을 기준으로 설계합니다. 상세 페이지 진입 없이 검색 결과 리스트에서 바로 확보 가능한 공개 사업장 정보만 수집합니다.

---

## 1. 구현 목표

- **목표**: 지역과 업종을 입력하면 모바일 네이버 지도 검색 결과 리스트에서 영업 대상 업체 정보를 수집하고, 중복 및 무전화 업체를 정리하여 Excel 파일로 저장한다.
- **타겟 URL 흐름**:
  - `https://m.map.naver.com/search.naver?query={keyword}`
  - 또는 모바일 웹 검색 흐름에서 동일하게 접근 가능한 검색 결과 리스트
- **V1 수집 방식**:
  - 모바일 웹 리스트 화면 중심 수집
  - iframe 접근 없음
  - 상세 페이지 클릭/진입 없음
  - 무리한 우회보다 리스트 화면 중심의 최소 수집 우선

---

## 2. V1 수집 필드

V1에서 수집하는 필드는 아래로 제한합니다.

- 업체명
- 업종
- 주소
- 대표전화
- 플레이스 URL
- 수집일

아래 항목은 V1에서 제외합니다.

- 리뷰 수
- 영업시간
- 평점
- 상세 페이지 내부 정보
- 대표자명
- 개인 휴대폰 번호
- 개인 이메일

---

## 3. 구현 태스크

### Task 1: 환경 구성 및 기본 구조 준비

1. `.venv` 생성 및 실행 가이드 정리.
2. `requirements.txt` 기준 의존성 설치.
3. `.env.example` 기준 기본 지역, 업종, 수집 딜레이 설정.
4. `output/` 저장 경로 준비.

### Task 2: 모바일 웹 리스트 수집기 개발 (`src/crawler.py`)

1. Playwright로 모바일 웹 검색 페이지를 로딩한다.
2. 검색어는 지역 + 업종 조합으로 만든다.
3. 검색 URL은 `m.map.naver.com/search.naver?query={keyword}` 흐름을 기준으로 한다.
4. PC 네이버 지도 `map.naver.com/v5/search`는 V1 대상에서 제외한다.
5. `searchIframe`, `entryIframe` 접근은 사용하지 않는다.
6. 검색 결과 리스트를 스크롤하며 리스트 화면에 노출된 업체 정보만 수집한다.
7. 상세 페이지 클릭/진입은 V1에서 제외한다.
8. 요청 간 딜레이를 두어 과도한 요청을 피한다.

### Task 3: 리스트 데이터 파서 개발 (`src/parser.py`)

1. 모바일 웹 리스트 DOM에서 업체명, 업종, 주소, 대표전화, 플레이스 URL을 추출한다.
2. 대표전화가 없는 업체는 영업 DB 가치가 낮으므로 제외 대상으로 처리한다.
3. 대표자명, 개인 휴대폰 번호, 개인 이메일은 수집하지 않는다.
4. 불필요한 공백, 줄바꿈, 중복 텍스트를 정리한다.

### Task 4: Excel 저장 모듈 개발 (`src/exporter.py`)

1. 수집 데이터를 DataFrame으로 변환한다.
2. 업체명 + 주소 기준으로 중복을 제거한다.
3. 대표전화가 없는 업체를 제외한다.
4. 수집일 컬럼을 추가한다.
5. 영업 관리용 기본 컬럼을 추가한다.
   - 영업상태
   - 연락담당자
   - 최근연락일
   - 메모
6. `output/` 디렉터리에 `{지역}_{업종}_영업DB_{수집일}.xlsx` 형식으로 저장한다.

### Task 5: 실행 진입점 통합 (`main.py`)

1. CLI 인자 또는 `.env` 설정으로 지역과 업종을 입력받는다.
2. 모바일 웹 리스트 수집, 파싱, 정제, Excel 저장 흐름을 통합한다.
3. 수집 결과 건수, 제외 건수, 저장 파일 경로를 콘솔에 출력한다.
4. 네트워크 오류, 빈 데이터, 셀렉터 변경 가능성에 대비한 예외 처리를 추가한다.

---

## 4. 검증 시나리오

1. `python main.py --region "강남구" --keyword "음식점"` 실행 시 모바일 웹 검색 결과 리스트에서 수집이 시작되는지 확인한다.
2. 수집 결과에 업체명, 업종, 주소, 대표전화, 플레이스 URL, 수집일만 포함되는지 확인한다.
3. 리뷰 수, 영업시간, 평점이 결과 파일에 포함되지 않는지 확인한다.
4. 상세 페이지 클릭/진입 없이 리스트 화면 중심으로 동작하는지 확인한다.
5. `searchIframe`, `entryIframe` 접근 코드가 사용되지 않는지 확인한다.
6. 대표전화가 없는 업체가 제외되는지 확인한다.
7. `output/` 폴더에 Excel 파일이 정상 생성되는지 확인한다.

---

## 5. 운영 원칙

- V1은 수집 안정성을 위해 리스트 화면 중심의 최소 수집만 수행한다.
- 차단 회피를 위한 무리한 우회, 캡차 우회, 내부 API 우회는 구현 범위에 포함하지 않는다.
- 법적 리스크를 줄이기 위해 공개 사업장 대표 정보만 대상으로 한다.
- 광고성 문자/이메일 발송 기능은 V1 범위에 포함하지 않는다.
